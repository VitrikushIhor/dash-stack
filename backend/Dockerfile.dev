FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package*.json ./
COPY prisma ./prisma/

RUN npm install

# Copy source code
COPY . .

# Expose port
EXPOSE 8000

# Run in development mode with hot-reload
CMD ["npm", "run", "dev"]
